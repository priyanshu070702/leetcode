class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
        map<int,int>res;
        for(int i=0;i<nums.size();i++){
            res[nums[i]]++;
        }
        for(auto a:res){
            if(a.second==1)return a.first;
        }
        return -1;
    }
};
